<span id="probe-result" class="text-xs ml-2 {% if ok %}text-success{% else %}text-danger{% endif %}">
  {% if ok %}probe ok — {{ count }} job{{ "" if count == 1 else "s" }} visible{% else %}probe failed: {{ error }}{% endif %}
</span>
