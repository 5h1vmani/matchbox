[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "matchbox"
version = "0.2.0"
description = "AI-assisted job application pipeline — scan, score, tailor, track."
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.49.0",
    "pydantic>=2.7",
    "typer>=0.12",
    "rich>=13.7",
    "ruamel.yaml>=0.18",
    "httpx>=0.27",
    "python-dotenv>=1.0",
    "jinja2>=3.1",
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "python-multipart>=0.0.12",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "mypy>=1.10",
    "ruff>=0.4",
    "types-PyYAML",
    "httpx>=0.27",  # also used by FastAPI TestClient
]

[project.scripts]
matchbox = "matchbox.cli:app"
matchbox-web = "matchbox.web.app:run"

[tool.hatch.build.targets.wheel]
packages = ["src/matchbox"]

[tool.ruff]
src = ["src"]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["src/matchbox"]
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
