<div id="run-started" class="border border-success bg-surface rounded p-3 text-sm mb-3">
  <div class="font-medium text-success">Run {{ run_id }} is ready.</div>
  <div class="text-xs text-text-muted">
    {{ job_count }} job{{ "" if job_count == 1 else "s" }} queued in
    <code>{{ work_queue_path }}</code>.
  </div>
  <div class="text-xs mt-2">
    Open a terminal and start Claude Code. Paste:
    <div class="relative mt-1">
      <pre id="brain-prompt-process-{{ run_id }}" class="bg-bg border border-border rounded text-[11px] p-2 pr-16 select-all">process run {{ run_id }}</pre>
      <button
        type="button"
        class="absolute top-1 right-1 text-[10px] text-text-muted border border-border rounded px-2 py-0.5 bg-surface hover:text-text"
        data-copy="#brain-prompt-process-{{ run_id }}"
      >Copy</button>
    </div>
    Then watch progress at <a class="text-accent underline" href="/review-run/{{ run_id }}">/review-run/{{ run_id }}</a>.
  </div>
</div>
