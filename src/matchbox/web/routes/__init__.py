"""HTTP route modules — one per concern (single responsibility).

- pages: full-page renders
- jobs: per-job HTMX endpoints (detail, state, star, response, tailor)
- bulk: multi-select bulk actions
- profile: profile editor + live re-score preview
- files: secure PDF/file serving
- system: health, demo seed, profile bootstrap
"""
