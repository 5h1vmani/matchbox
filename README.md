# Matchbox v0.2

AI-assisted job application pipeline. Scan, score, tailor, track.

**Status:** Work in progress — v0.2 rebuild.

## Quick start

```bash
pip install -e ".[dev]"
matchbox --help
```

See [docs/setup.md](docs/setup.md) for full setup instructions.
