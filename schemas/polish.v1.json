{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://matchbox.local/schemas/polish.v1.json",
  "title": "Matchbox polish payload (brain → app)",
  "description": "Written by the brain after the CV is assembled and the coverage report flags missing keywords. Lists the selected bullets whose wording should be rephrased to carry the missing must-have terms. The deterministic side validates each new text against shared/voice-rules.json, replaces the bullets in cv.json, re-renders cv.pdf, and re-runs the keyword-presence check.",
  "type": "object",
  "required": ["schema_version", "run_id", "job_id", "polished"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "integer", "const": 1 },
    "run_id": { "type": "string" },
    "job_id": { "type": "integer", "minimum": 1 },
    "polished": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "text"],
        "additionalProperties": false,
        "properties": {
          "id": {
            "type": "integer",
            "minimum": 1,
            "description": "Bullet id that was selected for this CV. Polishing a non-selected bullet is rejected."
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "New wording. Must satisfy shared/voice-rules.json."
          },
          "original_text": {
            "type": "string",
            "description": "Optional: what the brain saw before rephrasing. Surfaced in changes.md."
          },
          "covers": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Must-have keywords this rephrasing was intended to carry. Used to validate intent."
          }
        }
      }
    }
  }
}
